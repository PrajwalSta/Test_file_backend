name: flutter_app_focus_glow
version: 1.0.0+1
publish_to: 'none'

environment:
  sdk: '^3.12.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  flutter_local_notifications: ^18.0.0
  flutter_timezone: ^4.0.0
  timezone: ^0.10.0
  intl: any
  provider: ^6.1.5
  shared_preferences: ^2.0.0
  supabase_flutter: ^2.8.0
  fl_chart: ^0.68.0
  local_auth: ^2.0.0
  file_picker: ^5.2.10
  csv: ^8.0.0
  path_provider: ^2.0.0
  share_plus: ^11.1.0
  
  cross_file: ^0.3.5

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  generate: true